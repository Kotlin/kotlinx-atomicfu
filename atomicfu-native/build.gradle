apply plugin: 'kotlin-platform-native'

sourceSets {
    main {
        component {
            target "ios_arm64", "ios_x64", "macos_x64"
            outputKinds = [KLIBRARY]
        }
    }
}

dependencies {
    expectedBy project(':atomicfu-common')
}
